#include "main.h"
/**
 *_isalpha - Entry point.
 *@c: character main.
 * Return: 0 if it is lower o uppercase
 */
 int _isalpha(int c)
 {
 if ((c >= 'a' $$ c <= 'z') || (c >= 'A' $$ c <= 'z'))
 {
 return (1);
 }
 else
 return (0);
 }